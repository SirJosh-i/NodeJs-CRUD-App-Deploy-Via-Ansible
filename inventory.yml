# inventory.yml
all:
  hosts:
    myapp-server:
<<<<<<< Updated upstream
      ansible_host: 3.110.182.216  # Use variable instead of hardcoded IP
      ansible_user: "ubuntu"  # Use variable for user
      ansible_ssh_private_key_file: ~/.ssh/id_ed25519  # Use variable for key path
=======
      ansible_host: "{{ server_ip }}"  # Use variable instead of hardcoded IP
      ansible_user: "{{ server_user}}"  # Use variable for user
      ansible_ssh_private_key_file: "{{ ssh_key_path }}"  # Use variable for key path
>>>>>>> Stashed changes
      ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
